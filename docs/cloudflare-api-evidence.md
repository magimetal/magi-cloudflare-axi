# Cloudflare API evidence

- REST base: `https://api.cloudflare.com/client/v4`; GraphQL endpoint: `/graphql`.
- Direct API auth: bearer API token, or `X-Auth-Key` plus `X-Auth-Email` global-key headers.
- Hosted MCP source snapshot: `70ff690553722f731849ede6ba9ce98958395a23`.
- Pinned registered-name extractor and catalog both return 172 unique names. Extractor resolves names at registration call sites; declaration-only `hyperdrive_config_create` is excluded. Canonical schema compiler resolves all 172 registration-input contracts without executing upstream TypeScript.
- Pinned `apps/ai-gateway/src/auth-integration.spec.ts` sends direct stateless requests with `MCP-Protocol-Version: 2026-07-28`, `Mcp-Method`, optional `Mcp-Name`, and `_meta` client/protocol fields; response has no session ID.
- Current Cloudflare Agents documentation lists `https://mcp.cloudflare.com/mcp`, bearer API-token automation, `search`/`execute`, product server URLs, and stateless compatibility.
- Current Cloudflare source marks AutoRAG, Radar, and GraphQL product servers deprecated in favor of unified Cloudflare API MCP; server metadata reports this while preserving access.
- Current Claude and Codex hook docs require event → matcher group → command handler nesting. Current Codex feature key is `hooks`; current OpenCode docs confirm global plugin directory and plugin shell context.
- Remote HTTP is rejected except explicit loopback test endpoints. Endpoint userinfo, query, fragment, and traversal are rejected before credentials.
Phase 4G evidence: `list_rags` adds AutoRAG account scope with OAuth additions `account:read` and `rag:write`; no least-privilege token permission is claimed. Exact command is `magi-cloudflare-axi capability invoke list_rags --input '{}' --allow-egress`. Route is `GET /accounts/{account_id}/autorag/rags`, with query order `page`, `per_page`, defaults 1 and 20, bounded integer validation, strict full-response validation before `autorags`/numeric `total_count` projection, transient-read retries, redirect rejection, and 8 MiB response bound. Deprecated upstream server context does not imply operation unavailability.
Phase 4H evidence: `workers_builds_get_build` uses account auth and the exact command `magi-cloudflare-axi capability invoke workers_builds_get_build --input '{"buildUUID":"<buildUUID>"}' --allow-egress` for `GET /accounts/{account_id}/builds/builds/{buildUUID}` with no query or body. `buildUUID` is required, must be a non-empty string, and is locally limited to a safe single path segment of at most 256 bytes; UUID syntax is not claimed. A strict V4 envelope and the full pinned `BuildDetails` response, including ignored nested fields, are validated before a strict ten-field projection: `buildUUID`, normalized-ISO `createdOn`, `status`, nullable `buildOutcome`, `branch`, `commitHash`, `commitMessage`, `commitAuthor`, `buildCommand`, and `deployCommand`. A null result projects JSON null; environment variables and secret values are omitted and unknown output is stripped. Pinned app OAuth additions are `account:read`, `workers:read`, and `workers_builds:read`; these are not API-token permissions and do not establish least privilege. Non-metered, data-egress, and `transient_read` are local AXI policy classifications, not upstream facts. Transient GET retries, redirect refusal, credential/provider-message redaction where appropriate, and the 8 MiB response bound apply.
Phase 4I evidence: `workers_builds_list_builds` uses account auth and the exact command `magi-cloudflare-axi capability invoke workers_builds_list_builds --input '{"workerId":"<workerId>"}' --allow-egress` for one page only at `GET /accounts/{account_id}/builds/workers/{workerId}/builds?page={page}&per_page={perPage}`, with no body and defaults page 1/perPage 10. The pinned upstream input requires a nonempty `workerId`; local AXI validates a safe single segment up to 256 bytes and rejects whitespace, control characters, and path delimiters, without claiming additional `workerId` syntax. Local pagination safeguards constrain integer `page` to 1–10000 and `perPage` to 1–100`; the pinned upstream evidence specifies the query names and defaults, not those local bounds. A strict `success=true` V4 envelope and the full `BuildDetails` array, including nested categories, plus optional or null `result_info`, are validated before projection. Date values are validated as ISO strings or finite numeric milliseconds and normalized to ISO. The stable newest-first projection has exactly eight fields: `buildUUID`, `createdOn`, `status`, `buildOutcome`, `branch`, `commitHash`, `commitMessage`, and `commitAuthor`; environment variables, secret values, commands, and unknown output are omitted. A null `result` returns empty `builds` with null `pagination_info`; an empty array preserves valid `pagination_info`. Pinned app OAuth additions are `account:read`, `workers:read`, and `workers_builds:read`; these are app OAuth scopes, not API-token permissions or a least-privilege claim. Non-metered, data-egress, and `transient_read` are local AXI policy classifications, not upstream facts. The `--allow-egress` guard, transient GET retries, redirect refusal, credential/provider-message detail redaction, and 8 MiB response bound apply.
Phase 4I source evidence is pinned to commit `70ff690553722f731849ede6ba9ce98958395a23`: handler `apps/workers-builds/src/tools/workers-builds.tools.ts:13-76`, blob `3936684ab52f24fd02247b6a5e785f061b9bd2bd`, SHA-256 `59e614836b66369317d481f4f64c530c12f88b0a915b885b4e40a60b664cb01e`; API route `apps/workers-builds/src/api/workers-builds.api.ts:13-32`, blob `061f5240161acc5c2d355d968002e7a178df416b`, SHA-256 `3c35543e920f795a867d543e0a3700c2eeeca93d3d76ce36d535d6c0595b0db4`; input `apps/workers-builds/src/tools/workers-builds.tools.ts:21-25`, SHA-256 `81f7d85ef9411c94b45805d82a53eea32dd94c2d93dbe7611c55b466b1cb4c9d`; list types `apps/workers-builds/src/types/workers-builds.types.ts:66-80`, SHA-256 `2f5d1e8f6c7c90f0d50db43d65a35a5019bede5ce2eaee29f1f02cadea9f683c`; shared `BuildDetails` `apps/workers-builds/src/types/workers-builds.types.ts:3-64`, SHA-256 `a830f11089342d0ad203ee29f66e9eee6664cffad1386e7da2c1f1044e8bfd75`; and OAuth `apps/workers-builds/src/workers-builds.app.ts:21-28`, SHA-256 `e087ea416688217a28e509e26401a74ef76b53742cb97ca4f8244d6f93adf384`.


`capability schema` works offline, while `capability invoke` validates schema and route support before config/auth/network and accepts inline, file, or stdin JSON. Run `python3 scripts/catalog-governance.py validate`, `self-test`, and `check`; pinned CI recompiles and byte-compares schema artifacts.

Unknown product routes and generic SDK labels remain unverified until source-derived or official endpoint evidence exists. Registration-input schemas are complete for pinned source only; live hosted-server schemas may vary with request/auth context.

## Phase 0–4I catalog, schema, and operation contract
`capabilities/cloudflare-mcp-parity.json` uses schema version 3 with pinned repository/commit and denominator 172. Typed evidence provenance distinguishes pinned Git, official documentation, generated artifacts, hermetic tests, and missing evidence. Root `operation_artifacts` hash-binds 23 exact operation contracts: five Phase 2 representative slices, four Phase 3 Blog reads, seven Phase 4A–4C Browser reads, two Phase 4D binary Browser reads, one Phase 4E Logpush read, one Phase 4F Audit Logs read, one Phase 4G AutoRAG read, one Phase 4H Workers Builds detail read, and one Phase 4I Workers Builds list read. Current vector is `I=172; S=172; R=B=P=V=23; D=18; X=40`; 18 operations are discovery-verified and five remain discovery-generated. Reads are verified for 22 of 150 registrations. 149 routes remain unresolved for Phases 4–7.
`capabilities/cloudflare-input-schemas.json` contains 168 complete and 4 source-evidenced zero-input contracts; `capabilities/cloudflare-schema-fixtures.json` covers all 137 distinct raw-schema hashes with positive and negative instances. JSON Schema validation uses Draft 2020-12 metaschema and format assertions. Exact, version/hash-gated Zod 4.4.3 patterns preserve default email, UUID, ISO date, and UTC datetime acceptance. Ten URL-bearing contracts instead record two external-runtime facts that JSON Schema cannot execute: Zod trims input, then validates trimmed text with the JavaScript WHATWG URL constructor and returns trimmed text when `normalize=false`. The bundle also binds 803 typed dependency-provenance entries—ID/name, source file/blob, classification, source span kind/span, and source SHA-256—under aggregate SHA-256 `bd6c83d69c8464ec0d5b428a2631972aa1d30acabdf89f310b1a06f8d5678d04`; validators reject deletion, fabrication, nonempty unresolved state, and malformed source hash/blob/span even after contract and artifact hashes are rebound. No completion is inferred from schemas, method, path, transport, access class, implementation text, or blocker prose. Run `python3 scripts/catalog-governance.py validate`, `self-test`, and `check`; pinned CI recompiles and byte-compares schema artifacts.
