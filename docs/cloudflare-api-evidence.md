# Cloudflare API evidence

- REST base: `https://api.cloudflare.com/client/v4`; GraphQL endpoint: `/graphql`.
- Direct API auth: bearer API token, or `X-Auth-Key` plus `X-Auth-Email` global-key headers.
- Hosted MCP source snapshot: `70ff690553722f731849ede6ba9ce98958395a23`.
- Pinned registered-name extractor and catalog both return 172 unique names. Extractor resolves names at registration call sites; declaration-only `hyperdrive_config_create` is excluded. Canonical schema compiler resolves all 172 registration-input contracts without executing upstream TypeScript.
- Pinned `apps/ai-gateway/src/auth-integration.spec.ts` sends direct stateless requests with `MCP-Protocol-Version: 2026-07-28`, `Mcp-Method`, optional `Mcp-Name`, and `_meta` client/protocol fields; response has no session ID.
- Current Cloudflare Agents documentation lists `https://mcp.cloudflare.com/mcp`, bearer API-token automation, `search`/`execute`, product server URLs, and stateless compatibility.
- Current Cloudflare source marks AutoRAG, Radar, and GraphQL product servers deprecated in favor of unified Cloudflare API MCP; server metadata reports this while preserving access.
- Current Claude and Codex hook docs require event → matcher group → command handler nesting. Current Codex feature key is `hooks`; current OpenCode docs confirm global plugin directory and plugin shell context.
- Remote HTTP is rejected except explicit loopback test endpoints. Endpoint userinfo, query, fragment, and traversal are rejected before credentials.
Phase 4D evidence: authenticated `get_url_pdf` and `get_url_screenshot` join the prior 16 exact contracts. Phase 4 cohort now contains nine discovery-verified authenticated Browser reads; Browser family contains ten exact contracts when Phase 2's generated `get_url_markdown` is included. Global discovery is 13 verified and five generated. Vector: `I=172; S=172; R=B=P=V=18; D=13; X=40`; 154 routes remain unresolved. PDF is `POST /accounts/{account_id}/browser-run/pdf` with body `{url}`, response MIME `application/pdf`, and `%PDF-` signature. Screenshot is `POST /accounts/{account_id}/browser-run/screenshot` with body `{url,viewport}`, response MIME `image/png`, and PNG signature. Absent screenshot viewport is omitted because JSON cannot represent JavaScript `undefined`; supplied viewport applies nested width/height defaults 800×600 and strips unknown nested keys. Both require explicit new filesystem `--output`, refuse overwrite, create a private sibling temporary file before auth/network, enforce an 8 MiB maximum, never retry or follow redirects, and return artifact path, MIME type, byte count, and SHA-256. For `list_browser_sessions`, pinned handler `apps/browser-rendering/src/tools/browser.tools.ts:522-560`, blob `ae998f642ba8548b715e1573bc0049c96c9e1f28`, SHA-256 `c6b05861d44395a6e2bc84ac37320cd04d9a7edded73cf14d410fce32e31a361`, remains authority for `GET /accounts/{account_id}/browser-run/devtools/session`; official docs instead show `/browser-rendering/devtools/session` with optional `limit`/`offset`, while pinned zero-input handler exposes neither query. Phase 4 remains in progress.

`capability schema` works offline, while `capability invoke` validates schema and route support before config/auth/network and accepts inline, file, or stdin JSON. Run `python3 scripts/catalog-governance.py validate`, `self-test`, and `check`; pinned CI recompiles and byte-compares schema artifacts.

Unknown product routes and generic SDK labels remain unverified until source-derived or official endpoint evidence exists. Registration-input schemas are complete for pinned source only; live hosted-server schemas may vary with request/auth context.

## Phase 0–4D catalog, schema, and operation contract
`capabilities/cloudflare-mcp-parity.json` uses schema version 3 with pinned repository/commit and denominator 172. Typed evidence provenance distinguishes pinned Git, official documentation, generated artifacts, hermetic tests, and missing evidence. Root `operation_artifacts` hash-binds 18 contracts in `capabilities/cloudflare-operation-contracts.json`: five Phase 2 representative slices, four Phase 3 Blog reads, seven Phase 4A–4C Browser reads, and two Phase 4D binary Browser reads. Current vector is `I=172; S=172; R=B=P=V=18; D=13; X=40`; 13 operations are discovery-verified and five remain discovery-generated. Remaining 154 routes are unresolved for Phases 4–7.

`capabilities/cloudflare-input-schemas.json` contains 168 complete and 4 source-evidenced zero-input contracts; `capabilities/cloudflare-schema-fixtures.json` covers all 137 distinct raw-schema hashes with positive and negative instances. JSON Schema validation uses Draft 2020-12 metaschema and format assertions. Exact, version/hash-gated Zod 4.4.3 patterns preserve default email, UUID, ISO date, and UTC datetime acceptance. Ten URL-bearing contracts instead record two external-runtime facts that JSON Schema cannot execute: Zod trims input, then validates trimmed text with the JavaScript WHATWG URL constructor and returns trimmed text when `normalize=false`. The bundle also binds 803 typed dependency-provenance entries—ID/name, source file/blob, classification, source span kind/span, and source SHA-256—under aggregate SHA-256 `bd6c83d69c8464ec0d5b428a2631972aa1d30acabdf89f310b1a06f8d5678d04`; validators reject deletion, fabrication, nonempty unresolved state, and malformed source hash/blob/span even after contract and artifact hashes are rebound. No completion is inferred from schemas, method, path, transport, access class, implementation text, or blocker prose. Run `python3 scripts/catalog-governance.py validate`, `self-test`, and `check`; pinned CI recompiles and byte-compares schema artifacts.
